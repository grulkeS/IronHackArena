document.addEventListener("DOMContentLoaded", () => {
    let html = '';
    html += `<button name="nextRound">Next Round</button>`;
    html += `</div>`;

    document.querySelector("#startGameButton").innerHTML = html;
});